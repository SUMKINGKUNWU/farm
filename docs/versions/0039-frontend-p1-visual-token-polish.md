# 0039 前端 P1 视觉与状态收敛

## 当前已有内容

- `0038` 已修复管理台乱码、统一焦点样式，并补充玩家端玻璃浮层键盘体验。
- `0037` 审查中提出 P1：视觉 token 收敛、状态中文化、减少过度大圆角和大阴影。

## 本次完成内容

- 玩家端地块状态文案改为中文：
  - `Locked` 改为 `未扩建`。
  - `Open` 改为 `可投入`。
  - `Growing` 改为 `成长中`。
  - `Ready` 改为 `可收获`。
- 玩家端地块操作提示改为中文：
  - 农田空闲显示 `点击播种`。
  - 牧场空闲显示 `点击养殖`。
  - 未扩建显示 `先扩建栏位`。
  - 可收获显示 `点击收获`。
- CSS 增加语义 token：
  - `--focus`
  - `--success`
  - `--warning`
  - `--danger`
  - `--surface`
  - `--surface-raised`
- 收敛部分过度视觉效果：
  - 降低主容器和普通卡片的大圆角。
  - 降低普通卡片和游戏区域的阴影强度。
  - 保留底部页签和玻璃浮层的视觉重点，不破坏已确认的界面方向。

## 验证

- 已执行前端构建：`npm.cmd run build`。
- 构建通过。
- 仍存在 ECharts 大 chunk 警告，与历史情况一致。

## 下次建议

- 继续 P1：增强空状态和交易前置条件提示，尤其是未设置交易密码时的表单内提示。
- 开始 P2：动态导入 ECharts，减少玩家端首包体积。
- 做 390px、768px、1366px 三档响应式人工检查。
