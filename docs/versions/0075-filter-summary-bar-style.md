# 0075 筛选摘要条样式收口

## 本次调整

- 为前端筛选摘要增加统一的 `.filter-summary` 样式
- 采用轻量边框、浅底色和紧凑排版，适合放在表格元信息下方
- 这套样式已先用于玩家活动记录，后续管理端筛选摘要也可直接复用

## 变更文件

- `frontend/src/styles.css`

## 结果

- 当前筛选条件的可读性更高，不再只是裸文本
- 前端后续继续补筛选回显时，可以直接复用同一套摘要条视觉结构
