# 0034 玩家端游戏界面组件拆分

## 当前已有内容

- 玩家端游戏化界面已经落地到 `App.vue`。
- 后续细化了地块状态角标、底部页签和玻璃悬浮卡提示。
- `App.vue` 体积增长明显，继续维护会增加风险。

## 本次完成内容

- 新增独立组件：`frontend/src/components/GamePlayerWorkspace.vue`。
- 将玩家端游戏化界面从 `App.vue` 拆分到独立组件中。
- `App.vue` 改为导入并使用 `GamePlayerWorkspace`。
- 删除 `App.vue` 中旧的内联游戏界面组件，降低主文件体积。
- 将玩家端游戏界面表单状态移动到新组件内部，减少对 `App.vue` 全局表单对象的依赖。
- 修复 `App.vue` 顶部模板中的破损中文标签和破损链接。
- 修复管理员税率类型名称的破损字符串。

## 验证

- 已执行前端构建：`npm.cmd run build`。
- 构建通过。
- 仍存在 ECharts 大 chunk 警告，与历史情况一致，不影响本次拆分。

## 下次建议

- 继续拆分管理台组件，进一步降低 `App.vue` 复杂度。
- 将 `NoticeBlock`、`StatCard` 等通用组件抽到公共组件目录，避免玩家端和管理端重复定义。
- 逐步统一旧文件中的中文文案编码，减少后续维护时的乱码风险。
