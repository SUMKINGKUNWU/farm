# 0038 前端 P0 可读性与可访问性修复

## 当前已有内容

- `0037` 已完成前端页面审查和优化方案。
- P0 问题包括管理台乱码文案、缺少统一焦点样式、玻璃悬浮层键盘可用性不足。

## 本次完成内容

- 重写 `frontend/src/components/AdminWorkspace.vue` 模板文案：
  - 管理台标题、说明、按钮、表格列名、空状态全部改为正常中文。
  - 修复原有破损属性和乱码 label。
  - ECharts series 名称改为正常中文。
- 为玩家端玻璃悬浮层补键盘行为：
  - 打开浮层后自动聚焦关闭按钮。
  - 按 `Esc` 可关闭浮层。
  - 组件卸载时移除键盘监听。
- 增加全局 `focus-visible` 样式：
  - 覆盖按钮、输入框、下拉框、链接。
  - 使用高可见度焦点描边，提升键盘可用性。
- 增加 `prefers-reduced-motion: reduce` 支持：
  - 禁用非必要过渡和动画时长。
  - 关闭 hover transform，减少动效干扰。

## 验证

- 已执行前端构建：`npm.cmd run build`。
- 构建通过。
- 仍存在 ECharts 大 chunk 警告，与历史情况一致。

## 下次建议

- 继续处理 P1：收敛视觉 token，减少过度大圆角、大阴影和装饰性背景。
- 将玩家端地块状态从英文改为中文，并补形状或图标辅助，避免只靠颜色识别。
- 将管理台继续拆分为税率、令牌、资产、交易记录四个子面板。
