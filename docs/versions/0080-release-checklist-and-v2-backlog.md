# 0080 上线核对与第二版待办

## 本版上线结论

本版可以进入上线收尾。

当前没有发现必须阻断上线的问题，建议从现在开始冻结功能范围，只保留发版动作和必要的发布说明整理。

## 本版已完成范围

### 错误码一致性

- 前端已补错误码一致性测试
- 可稳定识别以下错误码并映射到统一 message / action：
  - `INSUFFICIENT_BALANCE`
  - `INSUFFICIENT_INVENTORY`
  - `STATE_CONFLICT`
  - `BULK_TOKEN_REQUIRED`
  - `BULK_TOKEN_INVALID`
  - `BULK_TOKEN_EXPIRED`
  - `BULK_TOKEN_LIMIT_EXCEEDED`

对应文件：

- `frontend/src/apiError.js`
- `frontend/src/apiError.test.js`

### 管理端筛选与列表

- 审计日志支持：
  - 动作筛选
  - 目标筛选
  - 原因建议
  - 原因关键词
  - 日期区间
  - 分页
  - 当前条件摘要
  - 重置日志
- 管理端交易记录支持：
  - 来源筛选
  - 状态筛选
  - 原因建议
  - 分页
  - 当前条件摘要
  - 重置交易
- 管理端玩家资产支持：
  - `itemType` 筛选
  - 当前条件摘要
  - 重置资产
  - 物品类型中文展示

### 玩家端活动记录

- 玩家交易记录与资产流水已走后端分页
- 玩家交易记录与资产流水支持原因筛选
- 已补当前条件摘要
- 已补重置交易 / 重置流水

### 管理端切换玩家行为

- 切换目标玩家时，会重置上一位玩家残留的：
  - 资产筛选
  - 交易筛选
  - 交易原因选项
  - 资产列表结果
  - 交易列表结果

## 本版已验证项

已实际验证：

```powershell
cd D:\ai-project\farm\frontend
npm.cmd run test -- apiError.test.js
npm.cmd run build
```

验证结论：

- 错误码一致性测试通过
- 前端生产构建通过

## 本版已知但不阻断上线

### 1. 前端包体仍有较大 chunk 警告

`vite build` 仍提示部分 chunk 超过 500 kB。

判断：

- 当前不影响功能正确性
- 不阻断本版上线
- 适合放到第二版做分包优化

### 2. 自动化回归覆盖仍偏窄

当前自动化验证重点集中在错误码一致性。

判断：

- 当前核心风险已被覆盖
- 但筛选、分页、切换玩家等行为还缺少前端级回归测试
- 不建议在本版上线前继续扩测并改动功能代码

## 第二版待办

### 建议继续做

1. 前端包体分包优化
2. 管理端切换目标玩家后的筛选重置回归测试
3. 审计日志筛选 / 分页回归测试
4. 玩家活动记录分页与筛选联动回归测试
5. 全站低频页面与提示文案的完整中文巡检

### 不建议现在继续做

以下内容不建议塞进本版：

- 再扩功能范围
- 大规模 UI 重构
- 为了消除 build 警告而临时调整架构
- 补一整套新测试框架

这些改动收益存在，但不适合作为当前上线前的最后一轮变更。
